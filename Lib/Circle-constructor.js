const ShapeRender = require("./shapes");

class Circle extends Shape {
  constructor(title, colorText, colorFill) {
    super(title, colorText, colorFill);
    this.shape = shape;
  }
  renderCirlcle() {}
}
